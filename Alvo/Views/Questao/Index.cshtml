@model IEnumerable<Alvo.ViewModels.QuestaoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table id="dvData" class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoQuestao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdCategoriaQuestao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdQuestionario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorBarema)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoQuestao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdCategoriaQuestao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdQuestionario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorBarema)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
